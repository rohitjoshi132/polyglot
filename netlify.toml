[build]
  base = "."
  command = "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/polyglot run build"
  publish = "artifacts/polyglot/dist/public"

[build.environment]
  NODE_VERSION = "20"

# Proxy /api/* to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://polyglot-api-okgo.onrender.com/api/:splat"
  status = 200
  force = true

# SPA Fallback: Redirect all unrecognized traffic to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
